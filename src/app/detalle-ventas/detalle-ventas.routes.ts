import { Routes } from '@angular/router';
// Importa tu componente principal de categorías
import { DetalleVentasComponent } from './detalle-ventas.component';

export const DETALLE_VENTAS_ROUTES: Routes = [
  { path: '', component: DetalleVentasComponent }
];